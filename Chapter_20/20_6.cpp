1.
less()

2.
彼此相邻

3.
size()

4.
没有地方

1.
multimap<ContactItem> myContacts;

2.
struct WordPropertyCase {
	bool operator()(const wordProperty& item1, const wordProperty& item2) const {
		return (item1.strWord < item1.strWord);
	};
};